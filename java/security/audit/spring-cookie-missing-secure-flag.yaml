rules:
- id: spring-cookie-missing-secure-flag
  message: |
    Inspired by https://find-sec-bugs.github.io/bugs.htm#INSECURE_COOKIE
  severity: WARNING
  languages: [java]
  patterns:
  - pattern-not-inside: $COOKIE.setSecure(true); ...
  - pattern: $RESPONSE.addCookie($COOKIE);
