rules:
- id: spring-csrf-disabled
  message: |
    CSRF is disabled for this configuration. This is a security risk.
    Do not do this unless there is a valid reason.
  severity: WARNING
  languages: [java]
  pattern: $OBJ.csrf(...).disable(...)
