server:
  port: 8081
management:
  endpoints:
    web:
      # ok: spring-actuator-fully-enabled-yaml
      base-path: /internal
      exposure:
        # ruleid: spring-actuator-fully-enabled-yaml
        include: "*"