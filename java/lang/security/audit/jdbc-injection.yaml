rules:
- id: jdbc-injection
  message: Possible JDBC injection detected
  patterns:
  - pattern: |-
      $JDBC = new JdbcTemplate();
      ...
      $JDBC.queryForObject("..." + $VAR + "...", ...);
  severity: WARNING
  languages:
  - java
