rules:
- id: my_pattern_id
  pattern: |
    gets(...)
  message: |
    gets function necessary leads to a memory corryption vulnerability. Replace it by fgets instead.
  severity: WARNING
  languages:
  - c
