rules:
    - id: hardcoded-jwt-key
      pattern: |
            var $X = []byte("...")
            ...
            $Y = token.SignedString($X)
      message: "JWT token is hardcoded"
      languages: [go]
      severity: WARNING