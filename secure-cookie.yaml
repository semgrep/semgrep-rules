rules:
- id: secure-cookie
  patterns:
  - pattern-not: flask.response.set_cookie(..., secure=True, httponly=True, ...)
  - pattern: flask.response.set_cookie(...)
  message: please, check that the secure=True and httponly=True are set.
  languages:
  - python
  severity: WARNING
  metadata:
    category: correctness
