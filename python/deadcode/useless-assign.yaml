rules:
  - id: useless-assignment
    patterns:
    - pattern: |
        $X = $Y
        $X = $Y
    message: "`$X` is uselessly assigned to the same value (`$Y`) twice"
    languages: [python]
    severity: WARNING
