rules:
  id: cai-javascript-framework-expressjs
  languages:
  - javascript
  message: Detected `express` being required and used, meaning this app uses the ExpressJS
    web framework.
  pattern: 'var $IMPORT = require("express");

    ...

    var $APP = $IMPORT();

    '
  severity: INVENTORY
