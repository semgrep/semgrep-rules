rules:
  id: cai-python-framework-flask
  languages:
  - python
  message: Detected `Flask()` being instantiated, indicating this app uses the Python
    Flask web framework
  pattern: '$FLASK = Flask(...)

    '
  severity: INVENTORY
