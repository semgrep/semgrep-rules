rules:
- id: flask-secure-set-cookie
  patterns:
  - pattern-not: |
      flask.response.set_cookie(..., httponly=True, secure=True,...)
  - pattern: |
      flask.response.set_cookie(...)
  message: |-
    this is just a test to make sure that I got the ruamel settings right
  severity: WARNING
  languages:
  - python
