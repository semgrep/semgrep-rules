rules:
  - id: detect-pytorch
    languages:
      - python
    severity: INFO
    message: "Possibly found usage of AI tooling: PyTorch"
    pattern-either:
      - pattern: import torch
      - pattern: torch.$FUNC(...)
    metadata:
      technology:
        - genAI
        - LLMs
      category: security
      confidence: LOW