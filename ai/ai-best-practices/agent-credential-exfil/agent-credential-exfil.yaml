rules:
  - id: agent-credential-exfil-generic
    languages: [generic]
    severity: ERROR
    message: >-
      An AI-agent credential or config surface (ANTHROPIC_API_KEY, CLAUDE_API_KEY,
      claude_desktop_config, .config/claude, .mcp.json) is read within a few hundred
      characters of an outbound network send (requests.post, fetch, urllib, axios, curl,
      wget) or a known exfiltration sink (webhook.site, Discord webhook). This is the
      credential-harvesting primitive seen in the Hades and Shai-Hulud supply-chain
      campaigns, where agent secrets are swept and shipped to an attacker host. Remove the
      exfiltration path, rotate any exposed key, and pin the dependency that introduced the
      code. Reading ANTHROPIC_API_KEY to call the real API is benign; only a secret read
      co-located with an outbound send to an arbitrary host is flagged.
    metadata:
      cwe: "CWE-522: Insufficiently Protected Credentials"
      category: security
      confidence: HIGH
      subcategory: [vuln]
      likelihood: HIGH
      impact: HIGH
      technology: [mcp, anthropic]
      owasp:
        - "LLM06:2025 Excessive Agency"
        - "LLM03:2025 Supply Chain"
      references:
        - https://socket.dev/blog/mini-shai-hulud-miasma-and-hades-worms-target-bioinformatics-and-mcp-developers-via-malicious
        - https://thehackernews.com/2026/06/hades-pypi-attack-19-packages-poisoned.html
        - https://github.com/Agent-Threat-Rule/agent-threat-rules/blob/main/rules/tool-poisoning/ATR-2026-00576-hades-agent-credential-theft.yaml
      source-rule-url: https://github.com/Agent-Threat-Rule/agent-threat-rules/blob/main/rules/tool-poisoning/ATR-2026-00576-hades-agent-credential-theft.yaml
    paths:
      include:
        - "*.py"
        - "*.js"
        - "*.ts"
        - "*.mjs"
    patterns:
      - pattern-regex: >-
          (?i)(?:ANTHROPIC_API_KEY|CLAUDE_API_KEY|claude_desktop_config|\.config[/\\]+claude|\.claude[/\\]+[a-z]|\.mcp\.json)[\s\S]{0,250}(?:requests?\.(?:post|put)|fetch\s*\(|urllib\.request|axios\.(?:post|get)|new\s+XMLHttpRequest|\bcurl\s|\bwget\s)|(?i)(?:requests?\.(?:post|put)|fetch\s*\(|urllib\.request|axios\.(?:post|get)|\bcurl\s+-|\bwget\s|exfiltrat\w*|webhook\.site|discord\.com[/\\]+api[/\\]+webhooks)[\s\S]{0,250}(?:ANTHROPIC_API_KEY|CLAUDE_API_KEY|claude_desktop_config|\.config[/\\]+claude|\.mcp\.json)
