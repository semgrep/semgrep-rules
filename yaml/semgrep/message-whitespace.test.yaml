rules:
  - id: half-written-crypto-example
    # ruleid: message-whitespace
    message: >-
      # ruleid: message-whitespace
      Semgrep found  a match
      # ruleid: message-whitespace
      I like    big space
    languages:
      - js
    severity: WARNING
    patterns:
      - pattern-inside: |
          const {...,createCipheriv,...} = require('node:crypto');
          ...
      - pattern: |
          createCipheriv($ALGO, $KEY, $IV);
