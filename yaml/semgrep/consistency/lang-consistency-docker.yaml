rules:
  - id: lang-consistency-docker
    patterns:
      - pattern: $X
      - pattern-either:
          - pattern-inside: |
              languages: ...
      - metavariable-regex:
          metavariable: $X
          regex: ^docker$
    message: >-
      Found '$X' in language config which diverges from semgrep.dev normalization.
    languages:
      - yaml
    severity: WARNING
    fix: dockerfile
    metadata:
      category: correctness
      technology:
        - semgrep
