version: "3.9"
services:
  # ok: privileged-service
  web:
    image: nginx:alpine
  worker:
    image: my-worker-image:latest
    # ruleid:privileged-service
    privileged: true
  # ok: privileged-service
  db:
    image: mysql
---
services:
  versionless:
    image: alpine:latest
    # ruleid: privileged-service
    privileged: true
---
name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      docker:
        image: docker:dind
        # ok: privileged-service
        privileged: true
